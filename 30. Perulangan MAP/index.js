function perulanganMap() {
    var kota = ['Palembang', 'Jakarta', 'Bali'];

    kota.map((item, index, array) => {
        console.log(item)
        console.log(index)
        console.log(array)
    })
}

perulanganMap()